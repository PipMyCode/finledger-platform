apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finledger-platform
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/PipMyCode/finledger-platform.git'
    path: k8s-manifests
    targetRevision: main
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
